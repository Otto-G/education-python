import socket

sock = sock.sock(socket.AF_INET, socket.SOCK_STREAM)
sock.connect(("localhost", 1234))

data = "foobar\n" * 10 * 1024 * 1024  # 70MB of data
assert sock.send(data) == len(data)  # True
